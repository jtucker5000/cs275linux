- name: Setup web servers
  hosts: web
  become: true

  tasks:
    - name: Install nginx
      apt:
        name: nginx
        update_cache: yes

    - name: Start nginx
      service:
        name: nginx
        state: started
